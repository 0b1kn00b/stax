package stx.fnc.mnd;

import stx.Outcome;

using stx.Continuation;

import stx.fnc.Monad;

class Promise<T> extends Base<T>{
  private var future : FutureType<Outcome<T>>;
  public function new(future){
    this.future = future;
  }
  /*public function flatMap<U>(fn:T->Monad<U>):Monad<U>{
    return future.flatMap(fn);
  }*/
}