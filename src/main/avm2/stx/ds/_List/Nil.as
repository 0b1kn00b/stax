package stx.ds._List {
	import stx.ds.List;
	import flash.Boot;
	public class Nil extends stx.ds.List {
		public function Nil(tools : * = null) : void { if( !flash.Boot.skip_constructor ) {
			super(tools);
		}}
		
	}
}
